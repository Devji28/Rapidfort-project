import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // Optional CSS if you are using Tailwind or custom styles
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
